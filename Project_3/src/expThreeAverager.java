import java.util.ArrayList;
import java.util.List;

/**
 * Created by Ben_Big on 2/25/17.
 */
public class expThreeAverager {
    /*
    public void addResult(double time, List<Float> result){
        if (time<=TCPStartTime) return;
        if (!timeToResult.containsKey(time)) {
            List<Float> newResult=new ArrayList<>();
            for (int i=0;i<result.size();i++){
                newResult.add(i,result.get(i)/numTrials);
            }
            timeToResult.put(time, newResult);
        }
        else{
            List<Float> newResult=new ArrayList<>();
            for (int i=0;i<result.size();i++){
                newResult.add(i,result.get(i)/numTrials+ timeToResult.get(time).get(i));
            }
            timeToResult.replace(time,newResult);
        }
    }*/
}
